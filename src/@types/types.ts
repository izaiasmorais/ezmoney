export type TransactionProps = {
  title: string;
  price: number;
  description: string;
  type: string;
  createdAt: Date;
  id: string;
};

export type FormDataProps = {
  title: string;
  price: number;
  description: string;
};
